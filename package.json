{
  "name": "laurajunco-xyz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "assemble": "node scripts/assemble.js",
    "build": "npm run assemble && eleventy",
    "serve": "npm run assemble && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "markdown-it": "^13.0.2"
  }
}

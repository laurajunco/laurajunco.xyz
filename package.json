{
  "name": "laurajunco-xyz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "assemble": "node scripts/assemble.js",
    "build": "node scripts/assemble.js --copy && eleventy",
    "serve": "npm run assemble && eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "dotenv": "^17.4.2",
    "markdown-it": "^13.0.2"
  }
}
